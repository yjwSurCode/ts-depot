{
  "name": "ts-depot-master",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna exec --scope ts-depot yarn build",
    "publish": "lerna publish"
  },
  "useWorkspaces": true,
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "version": "0.0.1"
}
